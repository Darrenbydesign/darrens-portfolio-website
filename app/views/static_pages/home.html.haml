#carousel 
  %ul 
    - @images.each do |i|
      %li
        = link_to(image_tag("#{i}", :alt => "#{i}".sub(/^[^\/]+\//,"")), portfolio_path)
%section#RecentPosts
  %h1.txt-center Recent Posts
  = render 'posts/feed_link'
  = render @posts_render, :summary => true 