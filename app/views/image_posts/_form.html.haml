= form_for @image_post do |f|
  - if @image_post.errors.any?
    #error_explanation
      %h2="#{pluralize(@image_post.errors.count, "error")} prohibited this image_post from being saved:"
      %ul
        - @image_post.errors.full_messages.each do |msg|
          %li= msg
  .actions
    = f.submit 'Save'
