= render 'feed_link'

%section#posts
  .page-header
    %h1 My Thoughts
    
  - if collection.present?
    = render collection, :summary => true
    = paginate collection
  - else
    No posts found.
